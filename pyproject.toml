[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scitex-dataset"
version = "0.7.1"
description = "Multi-domain scientific dataset fetcher — neuroscience, biology, pharmacology, medical (OpenNeuro, DANDI, PhysioNet, GEO, ChEMBL, ClinicalTrials.gov)"
readme = "README.md"
license = "AGPL-3.0-only"
requires-python = ">=3.10"
authors = [
    { name = "Yusuke Watanabe", email = "noreply@scitex.ai" },
]
keywords = [
    "neuroscience",
    "datasets",
    "openneuro",
    "dandi",
    "physionet",
    "bids",
    "nwb",
    "mri",
    "eeg",
    "fmri",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "httpx>=0.24.0",
    "click>=8.0.0",
    # HARD dependency, not an extra. The dataset index is `scitex_dev.store`
    # — there is no local fallback and nothing to degrade to, so a lean
    # install that lacked it would import fine and fail at the first search.
    # The floor is 0.57.0 because that is where the store's read-by-criteria
    # surface (`Store.search` / `Query` / `Schema(text_search=...)`) landed.
    "scitex-dev>=0.57.0",
    "scitex-config>=0.3.0",
]

[project.optional-dependencies]
mcp = [
    "fastmcp>=2.0.0",
]
huggingface = [
    "huggingface-hub>=0.20.0",
]
dev = [
    "fastmcp>=2.0.0",
    "huggingface-hub>=0.20.0",
    "pre-commit>=3.5.0",
    "pytest-cov>=4.0.0",
    "pytest>=7.0.0",
    "ruff>=0.1.0",
]
docs = [
    "sphinx>=7.0",
    "sphinx-rtd-theme>=2.0",
    "myst-parser>=2.0",
    "sphinx-copybutton>=0.5",
    "sphinx-autodoc-typehints>=1.25",
    "sphinx-click>=5.0",
]
all = [
    "scitex-dataset[mcp]",
    "scitex-dataset[huggingface]",
    # PS-221 closure: every public extra is a subset of `all`, so
    # `pip install scitex-dataset[all]` pulls everything public.
    "scitex-dataset[dev]",
    "scitex-dataset[docs]",
]

[project.scripts]
scitex-dataset = "scitex_dataset._cli:main"

[project.entry-points."scitex_dev.gate.checks"]
scitex-dataset = "scitex_dataset._gate_plugin:provide"

[project.entry-points."scitex_dev.docs"]
scitex-dataset = "scitex_dataset"

[project.entry-points."scitex_dev.skills"]
scitex-dataset = "scitex_dataset"

[project.urls]
Homepage = "https://github.com/ywatanabe1989/scitex-dataset"
Documentation = "https://scitex-dataset.readthedocs.io"
Repository = "https://github.com/ywatanabe1989/scitex-dataset.git"
Issues = "https://github.com/ywatanabe1989/scitex-dataset/issues"

[tool.hatch.build.targets.wheel]
# ``packages = ["src/scitex_dataset"]`` already pulls in every file
# under the package directory — including non-.py assets such as
# ``_sphinx_html/`` (the in-wheel docs bundle scitex-cloud serves from
# per PS-121) and ``_skills/``. A separate ``force-include`` rule for
# ``_sphinx_html`` would re-add every file a second time, producing a
# wheel with duplicate ZIP entries that PyPI rejects with
# ``400 Bad Request: Invalid distribution file. ZIP archive not
# accepted: Duplicate filename`` (observed on the v0.3.10 release run).
# So we deliberately rely on the implicit package include alone.
packages = ["src/scitex_dataset"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"
pythonpath = ["src"]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.coverage.run]
parallel = true
source = ["scitex_dataset"]
branch = true
concurrency = ["multiprocessing", "thread"]
omit = ["*/__pycache__/*", "*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
show_missing = true

[tool.scitex-linter]
library_dirs = ["src", "tests", "examples"]

[tool.scitex_dev.audit]
# PS-168 — Per-package secret + env-var prefix in GitHub Actions workflows.
# `GH_PERSONAL_ACCESS_TOKEN` is the ecosystem-wide cross-cutting PAT used
# by the contributor-assistant CLA action. The natural GITHUB_* namespace
# is reserved by GitHub Actions on the secret-name surface (HTTP 422 on
# every `gh secret set`), so the GH_ shorthand is the canonical
# substitute and is registered ecosystem-wide via PR #122 on scitex-dev
# (EXCEPTION_SECRETS_DEFAULT). This per-package entry pins the same
# exception inline so the audit passes on every scitex-dev version,
# independent of release timing.
ps168_secret_exceptions = ["GH_PERSONAL_ACCESS_TOKEN"]
